<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="megaminds.findmedicine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splashscreen.SplashScreenLauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Aboutus" />
        <activity android:name=".Search"></activity>
        <activity android:name=".WelActivity"></activity>
        <activity android:name=".SingleActivity"></activity>
        <activity android:name=".search.SearchBarMediaActivity"></activity>
    </application>

</manifest>
